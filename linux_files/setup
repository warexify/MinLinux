# placeholder to transition old github-based wlinux-setup upgrade

# add wf repos
# update
# install wlinux-base (with force flag set to allow install with base-files installed)
# install wlinux-setup 
# apt-mark hold base-files
# clean up